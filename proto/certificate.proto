syntax = "proto3";
option go_package = "github.com/alvidir/filebrowser/proto";

package proto;
import "proto/file.proto";

message Certificate {
    string id = 1;
    Permissions permissions = 2;
    string token = 3;
}

service CertificateService {
    rpc Get(File) returns (Certificate); 
}