syntax = "proto3";
option go_package = "github.com/alvidir/filebrowser/proto";

package proto;

message NewDirectory {}

message DirectoryDescriptor {
    string id = 1;
}

service Directory {
    rpc Create(NewDirectory) returns (DirectoryDescriptor); 
}